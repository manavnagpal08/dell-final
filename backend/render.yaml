services:
  - type: web
    name: vyom-ai-backend
    env: python
    buildCommand: pip install -r requirements.render.txt
    startCommand: uvicorn app.main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: PYTHON_VERSION
        value: 3.10.0
